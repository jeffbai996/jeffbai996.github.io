# Production Supabase Configuration
# These values are injected at build time via GitHub Actions secrets
# Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in your GitHub repository secrets
# See: Settings > Secrets and variables > Actions > Repository secrets
